<?xml version="1.0"?>
<server>
	<world>
		<shortname>rebuildeq</shortname>
		<longname>Shin's RebuildEQ TEST Server</longname>
		<!-- <longname>RebuildEQ</longname> -->

		<!-- Only specify these two if you really think you need to.  (read: You don't) -->
		<!-- <address>52.34.152.57</address> -->
		<!-- <localaddress>192.168.0.5</localaddress> -->

		<!-- Loginserver information.  Defaults shown -->
		<loginserver1>
			<host>login.eqemulator.net</host>
			<legacy>1</legacy>
			<port>5998</port>
			<account></account>
			<password></password>
		</loginserver1>
		<loginserver2>
			<legacy>1</legacy>
			<host>localhost</host>
			<port>5998</port>
			<account></account>
			<password></password>
		</loginserver2>

		<!-- Server status.  Default is unlocked -->
		<locked/>

		<!-- <unlocked/> -->

		<!-- Sets the ip/port for the tcp connections.  Both zones and console (if enabled).  Defaults are shown -->
		<telnet ip="127.0.0.1" port="9000" telnet="enabled"/>
		<tcp ip="127.0.0.1" port="9000" telnet="enabled"/>

		<!-- Sets the shared key used by zone/launcher to connect to world -->
		<key>oasijfoaisfjoij23%j2o3ij5o23i222</key>

		<!-- Enable and set the port for the HTTP service.  Defaults are shown -->
		<http port="9080" enabled="false" mimefile="mime.types" />
	</world>

	<!-- Chatserver (channels) information.  Defaults shown -->
	<chatserver>
		<host>127.0.0.1</host>
		<port>7778</port>
	</chatserver>

	<!-- Mailserver (in-game mail) information.  Defaults shown -->
	<mailserver>
		<host>127.0.0.1</host>
		<port>7778</port>
	</mailserver>

	<zones>
		<defaultstatus>20</defaultstatus>

		<!-- Sets port range for world to use to auto configure zones -->
		<ports low="7000" high="7100"/>
	</zones>

	<!-- Database configuration, replaces db.ini.  Defaults shown -->
	<database>
		<host>mariadb</host>
		<port>3306</port>
		<username>eqemu</username>
		<password>eqemupass</password>
		<db>eqemu</db>
	</database>

	<qsdatabase>
		<host>mariadb</host>
		<port>3306</port>
		<username>eqemu</username>
		<password>eqemupass</password>
		<db>eqemu</db>
	</qsdatabase>

	<!-- Launcher Configuration -->
	<launcher>
		<!-- <logprefix>logs/zone-</logprefix> -->
		<!-- <logsuffix>.log</logsuffix> -->
		<!-- <exe>zone.exe or ./zone</exe> -->
		<!-- <timers restart="10000" reterminate="10000"> -->
	</launcher>

	<!-- File locations.  Defaults shown -->
	<files>
		<!-- <spells>spells_us.txt</spells> -->
		<!-- <opcodes>opcodes.conf</opcodes> -->
		<!-- <logsettings>log.ini</logsettings> -->
		<!-- <eqtime>eqtime.cfg</eqtime> -->
	</files>
	<!-- Directory locations.  Defaults shown -->
	<directories>
		<maps>maps</maps>
		<!-- <quests>quests</quests> -->
		<!-- <plugins>plugins</plugins> -->
	</directories>

<discord>
        <username>shinnoir@hotmail.com</username>
        <password>nothing</password>
	<telnetusername>xackery</telnetusername>
	<telnetpassword>nothing</telnetpassword>
        <serverid>138438371700637696</serverid>
        <channelid>142522212203626496</channelid>
        <refreshrate>15</refreshrate>
	<itemurl></itemurl>
</discord>

	<github personalaccesstoken="783ae8581cc90d12b701b3f5fa76f60a187b9af5" repouser="Xackery" reponame="rebuildeq" issuelabel="ingame" characterlabel="character" npclabel="npc" itemlabel="item" refreshrate="500"/>
</server>
