# Bottle of Experience

sub EVENT_ITEM_CLICK {
	$client->Message(15, "This bottle contains "); #.$item->custom_data." experience.");
#GetCustomItemData()
	#if($itemid != 100000){
#		return;
	#}
	#$client->Message(15, "This bottle contains "); #.$item->custom_data." experience.");
}