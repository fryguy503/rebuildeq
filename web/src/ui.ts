
export function OnWindowLoadUI() {	
	console.log("loaded ui");
	//let categoryElements = document.getElementsByClassName("categories")
	//for (let catEle of categoryElements) {
	//	this.element.addEventListener("mousedown", (e: MouseEvent) => { this.onMouseDown(e); }, false)
	//}
}
